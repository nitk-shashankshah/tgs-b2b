services:
  backend:
    build: ./server
    env_file:
      - ./server/.env
    expose:
      - "4000"
    restart: unless-stopped

  frontend:
    build: .
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend
    restart: unless-stopped
